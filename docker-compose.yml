version: '3.5'
services:

  workers:
    image: wp1bot-workers
    container_name: wp1bot-workers
    volumes:
      - /data/wp1bot:/usr/src/app/lucky
    links:
      - redis
    restart: always
    depends_on:
      - redis

  redis:
    image: redis
    container_name: wp1bot-redis
    expose:
      - 6379
    restart: always
