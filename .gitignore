**/__pycache__
venv
docs-venv/
.tool-versions

# The credentials.py config system is gone (replaced by .env), but stale
# local copies and backups contain secrets and must never be committed.
wp1/credentials.py*

cookies.txt

# Don't commit test artifacts
**/cypress/videos
**/cypress/screenshots
.coverage

# Don't commit logs
log/**

# Don't commit the active .env file (contains secrets).
# .env.example and .env.docker are committed and safe to share.
.env

# Local agent instructions (may contain private infrastructure notes)
AGENTS.md


# Don't commit SSH keys
id_ed25519
id_rsa
*.pub

# Local git worktrees
.worktrees/

# Written by scripts/wp1/deploy-remote.sh on the production box
.last-deploy

# Flask session
flask_session/
