# Historical schema

`schema.sql` is a historical record of the database schema used by the tool
for what is referred to in code as the `wp10` database. It has been heavily
edited, but should be able to be used to re-create the `enwp10` database if
necessary.

The actual schema is versioned by the migrations in
[`db/migrations`](../db/README.md).
