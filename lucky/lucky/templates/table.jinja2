{% from 'format.jinja2' import format with context -%}
{| class="ratingstable wikitable plainlinks"  style="text-align: right; margin-left: auto; margin-right: auto;"
|- 
! colspan="{{total_cols}}" class="ratingstabletitle" | {{title}}
|-
! rowspan="2" style="vertical-align: bottom" | '''Quality'''
! colspan="{{total_cols - 1}}" | '''Importance'''
|-
{% for col in ordered_cols -%}
! {{col_labels[col]}}
{%- endfor %}
! style="text-align: center;" | '''Total'''
{% for row in ordered_rows -%}
|-
| {{row_labels[row]}}
{% for col in ordered_cols -%}
{%- if data[row][col] and data[row][col] > 0 -%}
|| {{ format(data[row][col], project, row, col) }}
{% else %}
||
{%- endif -%}
{%- endfor -%}
|| {{ format(row_totals[row], project, None, None) }}
{% endfor -%}
|-
| style="text-align: center;" | '''Total'''
{% for col in ordered_cols -%}
|| {{ format(col_totals[col], project, None, None) }}
{% endfor -%}
|-
{{'{{'}}User:WP 1.0 bot/WikiWork|project={{project}}|export=table{{'}}'}}
|-
|}
